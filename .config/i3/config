#1 i3 config file (v4)

set $mod Mod4

set $wallpaper ~/Pictures/wallpaper/spirited.jpg

font pango:source code pro 10
floating_modifier $mod

bindsym $mod+Return exec --no-startup-id st

bindsym $mod+Shift+q [con_id="__focused__" instance="^(?!dropdown).*$"] kill

bindsym $mod+d exec --no-startup-id i3-dmenu-desktop #dmenu_run #i3-dmenu-desktop

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# split in horizontal orientation
# bindsym $mod+Shift+v split h

# split in vertical orientation
bindsym $mod+t split toggle

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
# bindsym $mod+s layout stacking
bindsym $mod+w layout toggle
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space exec --no-startup-id $HOME/.config/i3/i3ipc_hideshow_border; floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Colors from Xresources
set_from_resource $bg  i3wm.background
set_from_resource $border  i3wm.color0  #f0f0f0
set_from_resource $fg  i3wm.foreground  #f0f0f0

# class                 border  backgr. text    indicator child_border
#client.focused          $border #121212 #c0c5ce $border   $border

client.background       #ffffff

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

# assign to workspace
# assign [class="Google-chrome"] 2
assign [class="Firefox"] 3
assign [class="Telegram"] 4
# assign [class="mpv"] 4
assign [class="Slack"] 4
assign [class="Transmission-gtk"] 5

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize grow width 5 px or 5 ppt
        bindsym k resize grow height 5 px or 5 ppt
        bindsym j resize shrink height 5 px or 5 ppt
        bindsym l resize shrink width 5 px or 5 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 5 px or 5 ppt
        bindsym Down resize grow height 5 px or 5 ppt
        bindsym Up resize shrink height 5 px or 5 ppt
        bindsym Right resize grow width 5 px or 5 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

set $sysmenu "system:  [k]ill [l]ock  [p]oweroff  [r]eboot  [s]uspend [h]ibernate"
bindsym $mod+Shift+e      mode $sysmenu

mode $sysmenu {
    bindsym k         exit
    bindsym l         exec --no-startup-id betterlockscreen -l dim, mode "default"
    bindsym s         exec --no-startup-id systemctl suspend, mode "default"
    bindsym p         exec shutdown -P now
    bindsym r         exec shutdown -r now
    bindsym h         exec --no-startup-id systemctl hibernate, mode "default"
    bindsym Return    mode "default"
    bindsym Escape    mode "default"
}

set $displayMode "Set display mode [l]aptop [h]ome [w]ork"
bindsym $mod+x mode $displayMode
mode $displayMode {

    bindsym h   exec --no-startup-id ~/.config/i3/scripts/home_display.sh; \
                mode "default"

    bindsym l   exec --no-startup-id ~/.config/i3/scripts/laptop_display.sh; \
                mode "default"

    bindsym w   exec --no-startup-id ~/.config/i3/scripts/work_display.sh; \
                mode "default"

    bindsym Return mode "default"
    bindsym Escape mode "default"
}

for_window [class="^.*"] border pixel 2
for_window [class="^.*"] move position center
smart_borders on

bar {
  font pango:Source Code Pro 8
  position top
  status_command i3blocks
  tray_output primary
  # tray_padding 2
}

## MEDIA CONTROLS ##
# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec --no-startup-id pamixer -i 5; exec --no-startup-id pkill -RTMIN+10 i3blocks                  # increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id pamixer -d 5; exec --no-startup-id pkill -RTMIN+10 i3blocks                  # decrease sound volume
bindsym XF86AudioMute exec --no-startup-id pamixer -t; exec --no-startup-id pkill -RTMIN+10 i3blocks                           # mute sound
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute 1 toggle    # mute mic

# Alsa Mixer Controls
#bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -q sset Master 5%+ #increase sound volume
#bindsym XF86AudioLowerVolume exec --no-startup-id amixer -q sset Master 5%- #decrease sound volume
#bindsym XF86AudioMute exec --no-startup-id amixer -q sset Master toggle # mute sound
#bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute 1 toggle # mute mic

# Sreen brightness controls
bindsym XF86MonBrightnessUp exec --no-startup-id xbacklight -inc 5      # increase screen brightness
bindsym XF86MonBrightnessDown exec --no-startup-id xbacklight -dec 5    # decrease screen brightness

# Media player controls
bindsym XF86AudioPlay exec --no-startup-id playerctl play-pause
bindsym XF86AudioNext exec --no-startup-id playerctl next
bindsym XF86AudioPrev exec --no-startup-id playerctl previous

## Screen shots ##
bindsym Print exec --no-startup-id scrot -u $HOME/Pictures/screenshots/$(date +%F_%T).png
bindsym --release Shift+Print exec --no-startup-id scrot -s $HOME/Pictures/screenshots/$(date +%F_%T).png -e 'xclip -selection c -t image/png < $f'

## ROFI PASS
bindsym $mod+p exec --no-startup-id passmenu

## Window Behavior
for_window [class="Firefox" title="Library"] floating enable
for_window [class="Firefox" title="Firefox Preferences"] floating enable
for_window [class="Google-chrome" title="Sign in - Google Accounts - Google Chrome"] floating enable
for_window [class="Google-chrome" title="Task Manager"] floating enable

popup_during_fullscreen leave_fullscreen

# Show the first scratchpad window
bindsym $mod+u      [instance="dropdown"] scratchpad show; [instance="dropdown"] move position center
for_window [instance="dropdown"] floating enable
for_window [instance="dropdown"] resize set 625 400
for_window [instance="dropdown"] move scratchpad
exec --no-startup-id st -n dropdown &

# Make mpv playing terminal float
for_window [instance="mpv_float"] floating enable
for_window [instance="mpv_float"] resize set 450 200

## DISABLE CTRL + Q
bindsym Control+q exec --no-startup-id /bin/false

## AUTO START ##
# exec_always --no-startup-id $HOME/.config/polybar/launch.sh
exec --no-startup-id feh --bg-fill $HOME/Pictures/wallpapers/spirited.jpg
exec --no-startup-id ibus-daemon --xim
exec --no-startup-id dunst
exec --no-startup-id compton --config ~/.config/compton/compton.conf -b
exec --no-startup-id udiskie -an &
exec --no-startup-id setxkbmap -option caps:swapescape
# exec --no-startup-id redshift
exec --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
# exec --no-startup-id nm-applet 
#exec --no-startup-id cmst
